const Icon = ({ icon }: any) => {
  return (
    <div style={{height: "32px", width: "32px", fontSize: "21px", display: 'flex', justifyContent: 'center', alignItems: 'center'}}>
      {icon}
    </div>
  )
}
export default Icon;